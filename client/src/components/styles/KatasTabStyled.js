import styled from "styled-components"

const KatasTabStyled = styled.section`
  font-size: 2rem;
  border-bottom: solid 1px rgba(255, 255, 255, 0.1);
  display: flex;
  justify-content: space-between;
  height: max-content;
  padding: 1rem;
`

export default KatasTabStyled
